package linkedlist.doublelist;

public class DListV2 {
    /*
    Sentinel:
    A special node that does not represent an item.
    DList v2: Circulary linked.

     */
    private DListNode head;
    private int size;

    //the sentinel previous points to tail and the sentinel's next points to head of the list an it's item is null
    //the next filed of the tail points to sentinel
    //the previous filed of the head points to sentinel also
    //the sentinel is a hidden part of this data structure


}
